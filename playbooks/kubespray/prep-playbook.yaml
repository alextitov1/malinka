- name: Prepare almaLinux nodes for kubespray
  hosts: kube_node:kube_control_plane
  gather_facts: false
  tasks:
    - name: Disable firewalld
      ansible.builtin.systemd:
        name: firewalld
        state: stopped
        enabled: false
